<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linecorp.myapp3.mapper.EntryMapper">
    <resultMap id="default" type="com.example.gyazo.entity.Image" autoMapping="true"/>

    <sql id="columns">
        id,
        hash,
        created_at
    </sql>

    <sql id="updateColumns">
        hash,
        created_at
    </sql>

</mapper>
